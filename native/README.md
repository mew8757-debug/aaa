# 성삼국지 촉한영걸전 - Native Android

이 디렉터리는 Winlator/Wine 의존성을 제거하고 게임을 Android 네이티브 앱으로 재구현하기 위한 작업 공간입니다.

## 목표

- APK 하나 설치 후 바로 실행
- Winlator, Wine, Windows 컨테이너 사용 안 함
- 기존 game1/game2의 데이터는 최대한 재사용
- 원작 전투/이벤트/맵 동작을 보존
- 터치 입력, 화면 비율, 저장 경로는 Android에 맞게 재설계

## 현재 구현 상태 (v4.84)

- 실제 첫 전투 맵 `map/m000.jpg` 네이티브 렌더링
- `RS/S_00.eex`의 플레이어/우군/적군 초기 배치 반영
- `Data.e5`의 CP949 한글 인물명 사용
- `Ekd5.exe`의 S형상 지정과 `Unit_mov.e5` / `Unit_atk.e5` / `Unit_spc.e5` / `Spalet.e5` 원본 스프라이트 연결
- `S_00.eex` 오프닝 이벤트의 대사, 등장/숨김, 이동, 회전, 동작, 퇴각, 지연, BGM/SFX 지시 순차 재생
- `Hexzmap.e5` M000 블록의 23x16 실제 지형 ID 사용
- `Data.e5` 상세병종의 실제 이동력과 병종계열별 30종 지형 이동비용 적용
- 가중치 기반 이동 가능 범위와 최소 이동비용 경로 적용
- `0x46/0x47` 실제 배치 레벨을 읽어 전투 패널 생성에 반영
- 인물 오능, 초기 HP, 상세병종 성장치를 전투 패널 원천값으로 보존
- HP / ATK / DEF 전투 상태와 원본 공격 모션 표시
- 공격 범위 ID 0(십자 인접), 1(주변 1칸) 대상 강조 및 터치 공격
- 첫 물리 피해 계산과 격파 처리
- 아군 턴당 1회 이동 + 공격 행동 상태
- 공격 후 생존 피격자의 사거리 내 반격
- 화면 우하단 `턴 종료` 버튼과 아군/적군 턴 전환
- 적군이 원본 지형 이동비용으로 가장 가까운 비적군 유닛을 추적해 이동/공격
- 다음 아군 턴 시작 시 이동/행동 상태 초기화
- `S_00.eex` 원본 1단계 목표: 우리군/우군이 `(6,13)` 마을에 도착
- 마을 도착 시 원본 Section 2-1의 백성5·추정·유비 이벤트와 목표 전환 재생
- 원본 `0x5D` 값에 따라 2단계 회합 상한 15턴 적용
- 원본 2단계 목표: 적군 전멸
- 유비(0)·추정(181) 사망, 1단계 적군 `(13,11)` 마을 진입, 턴 초과 패배 처리
- 전투 종료 시 승리/패배 상태로 입력과 AI 정지
- Scene 2 이벤트를 트리거·변수게이트·액션으로 빌드 시 컴파일
- 위치/영역/인접/HP/회합/행동 측/진영 인원수 트리거 지원
- `0x05` 참/거짓 변수 게이트와 `0x0B` 변수 설정 지원
- 적군/증원 구분을 원본 `0x47` 배치값에서 보존
- 미확인 액션이 섞인 Section은 fail-closed로 실행하지 않고 진단에 기록
- `0x4E` 원본 AI 정책 7종을 단일 인물/영역·진영 대상으로 적용
- 적군 자동 행동이 지정 무장·지정 좌표·추종 대상 등 원본 AI 목표를 반영
- `0x6D` 상대 복활/이동의 대상·기준 무장·상대 좌표·방향·복활 플래그 해석 및 실행
- S00 Section 9/10의 남자1(310)·노인2(309) 상대 위치 정렬 연출 재현
- `0x02` 무조건 중첩 시퀀스, `0x05` 변수 조건, `0x41` 진영 인원수 조건 재귀 실행
- `0x77` 정수변수 연산과 `0x3D` 보상 인벤토리 기록
- `0x4D` 전장 상태 변경 및 혼란(`0x08`) 행동 제한
- Section 11/12의 원본 일기토 명령 흐름을 대사·동작·공격 연출로 재생
- `0x03` else 중첩 분기 실행
- 적 전멸 시 Section 33 원본 승리 대사·도전 조건·전리품·변수 설정 실행
- Scene 3의 BGM 종료·메뉴 해제·팔레트 정리 후 S00 완료 상태 전환
- 원본 `R_01.eex` / `S_01.eex` 존재 여부와 Scene/Section/명령 구조 자동 진단
- 다음 시나리오의 전환 명령과 텍스트 샘플을 build-status에 기록
- `R_01` Scene 5의 7개 Section 전체 분기/출전 전환 트리 진단
- `0x12/0x13` 선택 분기와 `0x06/0x07` 출전 준비 흐름 파라미터 추가 기록
- S00 승리 후 `R_01` Scene 1~4 원본 스토리 연속 재생
- `0x12/0x13` 원본 2지선다를 Android 터치 선택 UI로 실행
- 대사·제목·장소·음악·효과음·보상·변수/전역값 변경을 원본 순서로 처리
- Scene 5 Section 1 출발 대사/출전 테스트/정리 후 `S_01` 준비 완료 상태 전환
- R 장면 배치/이동/배경 명령은 구조를 보존하되 전용 R 그래픽 렌더링은 아직 미적용
- `M001` 원본 지도와 Hexzmap entry 1 지형을 두 번째 전투 자산으로 패키징
- `S_01` Scene 1 Section 1의 0x4A/4B/46/47 초기 배치와 목표/턴/작전개시 자동 진단
- `battle1.json`에 S01 플레이어 3명·우군 10명·적군 29명과 M001/terrain1 패키징
- R01 Scene 5 종료 직후 battle1을 런타임 재로딩하여 S01 전투 실제 진입
- S01에서도 기존 지형 이동·물리 공격·반격·적 자동행동 엔진 재사용
- S01 20턴 제한 및 유비(0)·주준(118) 보호/적 전멸 기본 승패 조건 적용
- Scene2 이벤트 중 현재 명령 집합으로 완전 해석 가능한 Section만 자동 실행
- S01 `0x12/0x13` 선택 분기를 전투 중 터치 선택 UI로 실행
- S01 `0x3A` 전역값/금전/장절 계열 연산을 기존 globalValue 상태에 반영
- S01 `0x31` 범위/진영 숨김 실행
- S01 `0x6B` 좌표 법술 연출 실행
- S01 `0x58` 전장 물체/지형 설정 및 이동비용 즉시 반영
- S01 `0x78` 정수변수↔무장 HP/HpCur 속성 전송 실행
- leaf `0x12` + sibling `0x13` 선택 구조까지 Android 터치 UI로 실행
- S01 종료 Section 37/38/52/53/54와 Scene 3 Section 1 전체 트리 자동 진단
- 원본 `R_02.eex` / `S_02.eex` 다음 시나리오 구조 자동 진단
- S01 주준(118)·유비(0)·조조(36) 사망 전용 원본 패배 연출 실행
- S01 일반 패배/턴초과는 Section 54 원본 패배 흐름으로 처리
- 적 전멸 시 Section 53 후일담·전리품·변수 설정과 Scene 3 정리 실행
- 승리 후 `R_02` Scene 1~10 원본 스토리와 Scene 11 출전 흐름 재생
- R02 선택지/분기/전역값·변수 변경을 기존 스토리 실행기로 처리
- R02 완료 후 `S_02` 전투 준비 완료 상태로 전환
- `M002` 원본 지도와 Hexzmap entry 2 지형을 `terrain2.bin`으로 패키징
- `S_02` Scene1 Section1의 강제출전/플레이어 슬롯/우군/적군/목표/턴 제한 자동 진단
- `battle2.json` 생성 및 R02 종료 직후 S02 전투 실제 진입
- 유비·공손찬·간옹을 Data.e5 이름으로 역매칭해 보호 인물 ID 자동 확정
- S02에서도 공용 지형 이동·공격·반격·적 AI 및 지원 전장 이벤트 실행
- `0x78` 속성32를 원본 전장 인물 방향으로 해석해 정수변수↔방향 전송 지원
- R09 `0x3E` 가입 장비 설정을 원본 상대 인코딩으로 상태 보존·실행
- S02 Scene2 전장 이벤트 후보 30/30 완전 지원을 CI에서 검증
- S02 종료 후보 Section 20/21/31/33/34/37/38 및 Scene3 정리 전체 트리 진단
- 원본 `R_03.eex` / `S_03.eex` 다음 시나리오 구조 자동 진단
- S02 간옹(26) 사망 시 Section 20 전용 원본 패배 연출 실행
- S02 기타 보호인물 사망/턴초과/아군전멸 시 Section 38 원본 일반 패배 실행
- S02 적 전멸 시 Section 37 원본 승리 후일담·보상·변수 설정 실행
- S02 Scene 3 전투후 정리 후 `R_03` Scene 1~11 스토리 + Scene 12 출전 흐름 재생
- R03 완료 후 `S_03` 전투 준비 완료 상태 전환
- `M003` 원본 지도와 Hexzmap entry 3 지형을 `terrain3.bin`으로 패키징
- `S_03` Scene1 Section1 초기 배치/목표/턴/작전개시 자동 진단
- `S_03` Scene2 전장 이벤트 후보/지원률 자동 진단
- `0x6F` 원본 아이템 제거 명령을 공용 이벤트 실행기에 추가
- S03 승리/패배/Scene3 종료 후보 Section 자동 탐지 및 전체 트리 기록
- `battle3.json`에 M003/terrain3, 플레이어 3명·우군 9명·적군 39명 패키징
- 손견 원본 AI4 목표좌표를 `rescue-character` 승리조건으로 보존
- 유비·조조 사망/15턴 초과/아군전멸 패배와 원본 Section 24/25/31 연출 연결
- 손견 목표 도달 시 Section 30 승리 후일담 → Scene3 정리 실행
- R03 종료 후 battle3을 재로드해 실제 S03 전투 진입
- `0x11` 원본 시나리오 점프를 공용 이벤트 액션으로 실행
- S03 Scene3 정리의 점프 대상값과 `R_04/S_04` 원본 구조 자동 진단
- `R_04.eex` 부재와 `S_04.eex` 직접 존재 구조 확인
- `M004` 원본 지도와 Hexzmap entry 4 지형을 `terrain4.bin`으로 패키징
- `S_04` Scene1 초기 배치/목표/턴, Scene2 이벤트 지원률, 승패 후보 Section 자동 진단
- `battle4.json` 생성: M004/terrain4, 원본 S04 플레이어·우군·적군 배치
- S03 원본 `scenarioJump=8`에서 S04로 직접 진입
- S04 초기 목표 `화웅 처치`, 유비·관우·장비 보호, 5턴 제한 실행
- S04 Section39 승리, 11/18/19 특정 사망 패배, 40 일반패배, Scene3 정리 실행
- `0x53` mode1 범위·진영 퇴각 명령 지원
- S04 종료 뒤 원본 `R_05.eex` / `S_05.eex` 존재·구조 자동 진단
- S14 종료 뒤 원본 `R_15.eex` / `S_15.eex` 존재·Scene/Section/명령 구조 자동 진단
- `M015` 존재 시 JPEG 크기와 Hexzmap entry15 지형을 probe 자산으로 패키징
- S15 초기 플레이어 슬롯/우군/적군/목표/턴 제한과 Scene2 이벤트 지원률 자동 진단
- S15 승패 후보 Section을 compact build-status에 기록
- S14 승리 후 `R_15` Scene 1~5 스토리 + Scene 6 출전 흐름 실제 재생
- R15 고정/선택 출전 후보에서 S15 5인 roster 자동 구성
- `battle15.json`: M015 28×20/terrain15, 플레이어5·우군3·적군24 패키징
- R15 종료 직후 battle15을 로드해 실제 S15 전투로 연속 진입
- S15 전장 이벤트 17/17 완전 지원, 유비 보호·우군 생존·15턴·적 전멸 승패 조건 실행
- S15 Section15 유비 사망 패배, Section20 승리 후일담, Section21 일반패배, Scene3 정리 실행
- R 스토리 공용 컴파일러에 `0x11` 시나리오 점프 지원 추가로 R15 Scene5 완전 컴파일
- S15 종료 뒤 원본 `R_16.eex` / `S_16.eex` 존재·Scene/Section/명령 구조 자동 진단
- `M016` 존재 시 JPEG 크기와 Hexzmap entry16 지형을 probe 자산으로 패키징
- S16 초기 배치·목표·턴, Scene2 이벤트 지원률 및 승패 후보를 compact status에 기록
- S15 승리 후 `R_16` Scene 1 스토리 + Scene 2 출전 흐름 원본 액션으로 재생
- `R_16` 출전 후보/고정 인물을 원본 Section 순서로 5인 roster에 반영
- `M016`/`terrain16`/`battle16.json` 패키징 후 R16 종료 직후 실제 S16 전투 진입
- S16 Scene2 전장 이벤트 probe 11/11 지원, 유비 보호·우군 생존·15턴·적 전멸 승패 조건 적용
- S16 Section9 유비 사망 패배, Section14 승리 후일담, Section15 일반패배, Scene3 정리 실행
- S16 이후 원본 `R_17.eex` / `S_17.eex` 존재·Scene/Section/전환 명령 구조 자동 진단
- `M017` JPEG/Hexzmap entry17 지형 크기와 S17 초기 배치·목표·전장 이벤트 지원률/승패 후보 기록
- `R_05` Scene 1~8 스토리 + Scene 9 출전 흐름 원본 액션으로 컴파일
- `M005` 원본 지도와 Hexzmap entry 5 지형 패키징
- `S_05` 초기 배치/여포 격퇴 목표/이벤트 지원률/승패 후보 자동 진단
- S04 승리 후 `R_05` 9개 Scene 실제 재생 및 출전 흐름 실행
- `battle5.json`으로 S05 실제 진입: 여포(119) 격퇴, 20턴, 5인 보호
- S05 특정 패배(유비·관우·장비·원소·공손찬), 승리 후일담, 일반패배, Scene3 정리 연결
- S05 종료 뒤 원본 `R_06.eex` / `S_06.eex` 존재·구조 자동 진단
- `R_06` Scene 1~13 스토리 + Scene 14 출전 흐름 원본 액션으로 컴파일
- `M006` 원본 지도와 Hexzmap entry 6 지형 패키징
- `S_06` 초기 배치/문추 격퇴 목표/강제출전/이벤트 지원률/승패 후보 자동 진단
- R06 `0x3B` 무장 합류와 `0x67` 장명 표시를 스토리 실행기에 연결
- R06 14개 Scene 완전 컴파일 후 Scene14 출전에서 S06으로 실제 전환
- `battle6.json` 생성: 조운(4)·하후박(19) 조작, 공손찬(148) 보호, 문추(109) 격퇴
- S06 전장 이벤트 21/21 지원 및 20턴 제한 적용
- 문추 격퇴 Section13, 조운/하후박/공손찬 사망 패배, Section25/26 및 Scene3 정리 연결
- S06 종료 뒤 원본 `R_07.eex` / `S_07.eex` 존재·구조 자동 진단
- `R_07` Scene 1~9 스토리 + Scene 10 출전 흐름 완전 컴파일
- `M007` 원본 지도와 Hexzmap entry 7 지형 패키징
- `S_07` Scene1 초기 배치/목표/턴, Scene2 이벤트 및 승패 후보 자동 진단
- S06 승리 후 `R_07` Scene 1~9 + Scene 10 출전을 실제 연속 재생
- `battle7.json`: M007 22×21, 유비·관우·장비·간옹 4명 + 원본 우군/적군 배치
- S07 목표 `원소(101) 격퇴`, 보호 `유비(0)`, 30턴을 네이티브 승패 상태기로 실행
- S07 Section 55 승리/35 유비 사망/56 일반 패배/Scene3 정리 원본 연출 실행
- S07 종료 뒤 `R_08.eex` / `S_08.eex` 구조를 다음 확장용 probe로 기록
- S07 `0x36` 현재HP 비교를 >= / < / = 조건으로 일반화
- S07 `0x38` 현재HP 설정과 `0x5C` 무장 강조 연출 실행
- R07 마지막 `0x78`의 실제 파라미터를 build-status에 별도 기록
- R07 `0x78 [0,1,4,0]`을 정수변수0 → 인물4 R형상 속성 전송으로 실행
- R형상 상태를 전투 유닛과 분리해 스토리 전환 사이에 유지
- R07 전체 10 Scene을 `supported=true`로 컴파일해 S06→R07→S07 연속 진입 가능
- `R_08` Scene 1~22 스토리와 Scene 23의 6개 Section 출전/분기 전체 진단
- `M008` 원본 지도와 Hexzmap entry 8 지형 패키징
- `S_08` 초기 배치/목표/턴, 전장 이벤트 후보 및 승패 후보 자동 진단
- R08 `0x78`의 초상(속성1)·R형상(속성0) 정수변수 전송 실행
- S07 승리 후 R08 Scene 1~22 + Scene 23 출전 흐름을 실제 연속 재생
- `battle8.json`: M008 24×20, 유비·관우·장비·간옹·마운록 5명과 원본 우군/적군 배치
- S08 `0x72` 확장명령 11의 AI 행동영역 제한을 원본 좌표/인물 목록대로 적용
- S08 `0x38` 마운록 정신력 +10 패널 변경 이벤트 상태 보존
- S08 전장 이벤트 45/45 완전 지원을 CI에서 검증하도록 구성
- S08 유비(0)·공융(149) 보호, 20턴, 적 전멸 및 원본 승리/패배/Scene3 정리 연결
- 원본 `R_09.eex` / `S_09.eex` 존재·Scene/Section/명령 구조 자동 진단
- `M009` 원본 지도와 Hexzmap entry 9 지형 패키징
- `S_09` 초기 배치/목표/턴, Scene2 이벤트 지원률 및 승패 후보 자동 진단
- `R_09` Scene 1~17 스토리 + Scene 18 출전 흐름을 공용 스토리 실행기로 컴파일
- R09 출전 `0x06` 명령에서 S09 8인 플레이어 roster를 자동 추출
- `battle9.json`: M009/terrain9, 원본 우군·적군 배치, 유비·도겸 보호, 20턴 패키징
- S09 Scene2 추출에서 S00 전용 제외목록을 제거하고 종료 Section 외 전체 이벤트 재컴파일
- S08 승리 후 R09→S09 실제 연속 진입
- S09 조조 격퇴 Section51의 변수56을 원본 승리 신호로 연결
- S09 Section74 승리 후일담, 56/57 전용패배, 75 일반패배, Scene3 정리 실행
- S09 유비-도겸 인접은 Section1 발동 조건으로만 사용하고, Section1의 변수0=1·609=1 완료 후 직접승리 확정
- S09 조조 격퇴는 Section51 변수56 신호로 Section74 후일담 승리 루트 실행
- 두 승리 루트의 중복 후일담 재생을 방지하고 각각 원본 종료 순서를 유지
- 원본 `R_10.eex` / `S_10.eex` 존재·Scene/Section/명령 구조 자동 진단
- `M010` 존재 시 원본 지도 크기와 Hexzmap entry 10 지형을 probe 자산으로 패키징
- `S_10` Scene1 초기 배치/목표와 Scene2 이벤트·승패 후보를 자동 진단
- `R_10` Scene 1~25 스토리 + Scene 26 출전 흐름을 공용 R 스토리 컴파일러로 검증
- `S_10` `0x79` 정수변수 비교 조건을 네이티브 중첩 이벤트 실행기에 추가
- S10 전장 이벤트 43/43 완전 지원 여부를 CI에서 검증
- R10 `0x20` 무장 초상 상태 명령을 R 스토리 시각 상태로 보존해 26개 Scene 완전 컴파일
- R10 Scene26 `0x2D` 클릭 대상에서 S10 8인 출전 roster를 자동 추출
- `battle10.json`: M010 24×28/terrain10, 원본 플레이어 8명·적군 44명과 S10 이벤트 43/43 패키징
- S09 승리 후 R10 Scene 1~25 + Scene26 출전 흐름을 실제 재생하고 S10으로 연속 진입
- S10은 1단계 장훈/기령 격퇴 후 2단계 15턴 방어 구조를 보존하기 위해 `s10-event-driven` 모드로 진입
- v4.17 compact summary에서 R10 지원·8인 roster·battle10 구성·S10 43/43을 자동 검증
- S10 종료 Section 24/26/41/50/51 및 Scene3 Section1 전체 트리를 compact summary에 기록
- S10 기본 S형상이 유효하지 않은 256~259번 배치 인물의 원본 `0x75` 특수형상 지정 여부를 자동 추적
- 누락 배치를 임의 스프라이트로 대체하지 않고 원본 이벤트의 형상 교체 근거가 있을 때만 복원하도록 진단 강화
- S10 변수70 설정/검사, 장훈·기령 사망, 목표/턴제한 변경 Section을 전환 probe로 기록
- S10 종료 다음 `R_11.eex` / `S_11.eex` 존재·Scene/Section/명령 구조 자동 진단
- R10 변수70 경로: S10 Section 24/26 원본 조건으로 기령/장훈 격퇴 승리 실행
- R10 변수71 경로: 15턴 방어 후 S10 Section 50 원본 승리 실행
- 유비 사망 Section 41 / 일반 패배 Section 51 / Scene3 정리 실행
- S10 승리 후 `R_11` Scene 1~9 스토리 + Scene10 출전 흐름 연속 재생
- R11 종료 시 `S_11 전투 준비 완료` 상태까지 연결
- `M011` 원본 지도/Hexzmap entry 11 지형을 probe 자산으로 패키징
- `S_11` 초기 배치·목표·Scene2 이벤트 지원률·승패 후보 Section을 compact status로 자동 진단
- R11 Scene10 출전 roster 10명을 원본 `0x06`에서 자동 추출
- `battle11.json`: M011/terrain11, 플레이어10·우군4·적군71 및 S11 전장 이벤트 패키징
- R11 종료 직후 battle11을 재로드해 실제 S11 전투 진입
- 유비·관우·장비 사망 Section38/39/40, 승리51, 일반패배52, Scene3 정리 원본 연출 패키징
- 날이 밝음/광릉 점령 승리 발동 Section을 transitionProbe로 별도 기록
- S11 Section9의 25턴/아군 측 연출 완료 `변수9=1`을 날이 밝음 승리로 실행
- S11 Section10의 아군/우군 `(11,0)` 점령 완료 `변수16=1`을 광릉 점령 승리로 실행
- 두 승리 루트 모두 원본 Section51 후일담 → Scene3 정리 흐름으로 연결
- S11 다음 원본 `R_12.eex` / `S_12.eex` 존재·Scene/Section·명령 구조 자동 진단
- `M012` 존재 시 JPEG 크기와 Hexzmap entry 12 지형을 probe 자산으로 패키징
- R12 Scene1~21 스토리 + Scene22 출전을 기존 R 스토리 컴파일러로 검증
- R12 Scene22에서 실제 출전 후보 10인 roster 자동 추출
- S12 초기 플레이어 슬롯/우군/적군/목표/턴 제한과 Scene2 이벤트 지원률 자동 진단
- S12 승패 후보와 주요 전환 Section을 compact status에 기록
- S11 승리 후 R12 Scene1~21 스토리와 Scene22 출전 흐름을 실제 Android에서 연속 재생
- R12 종료 시 S12 준비 완료 상태 전환
- S12 종료 후보 Section의 루트 조건/핵심 본문을 compact status로 세부 기록
- `battle12.json`: M012 36×16, R12 출전 10인·우군3·적군59와 S12 전장 이벤트 패키징
- R12 Scene22 출전 종료 직후 실제 S12 전투로 연속 진입
- 원본 변수2 퇴각 루트: 유비 보호·20턴·서쪽 퇴각 또는 적 전멸 승리
- 원본 변수3 섬멸 루트: 유비·관우·장비 보호·25턴·적 전멸 승리
- S12 Section42/53/54 패배, 66/67 승리, 68 일반패배 및 Scene3 정리 실행
- S12 종료 다음 원본 `R_13.eex` / `S_13.eex` 존재·Scene/Section·명령 구조 자동 진단
- `M013` 존재 시 JPEG 크기와 Hexzmap entry 13 지형을 probe 자산으로 패키징
- S13 초기 플레이어 슬롯/우군/적군/목표/턴 제한과 Scene2 이벤트 지원률 자동 진단
- S13 승패 후보 Section을 compact build-status에 기록
- S12 승리 후 `R_13` Scene 1~26 및 Scene 27 출전 흐름 실제 재생
- R13 원본 출전 roster 7명을 S13의 7개 플레이어 슬롯에 자동 배치
- `battle13.json`으로 M013/S13 전투 실제 진입
- S13 기본 승패: 25턴, 유비(0)·조조(36) 보호, 적 전멸
- S13 `0x3B` 무장 합류를 전장 이벤트에서도 실행
- S13 `0x37` 금전/장절/충간 전역값 조건을 `>= / < / =` 원본 비교 순서로 실행
- S13 `0x0C` Section 즉시 종료와 `0x38` 최대HP 변경을 원본 의미대로 실행
- S13 승패 Section 39/50/76/77·Scene3 원본 트리를 compact status로 세부 기록
- S13 조조(36)·유비(0) 사망 전용 패배, Section76 승리, Section77 일반패배를 원본 연출로 실행
- S13 Scene3 전투후 정리 뒤 R_14 원본 스토리를 자동 Scene 수로 연속 재생
- R14 종료 시 S_14 전투 준비 완료 상태로 전환
- R_14.eex / S_14.eex 구조와 M014/Hexzmap entry14를 compact status에 자동 진단
- 256~259번은 S10 원본 `0x75` 특수형상 배정이 없음을 명시적으로 확인
- 대형 build-status와 별도로 `v4.16-summary.json`에 R10/S10 핵심 진단만 기록
- S07/S08 상태 판정 중첩 회귀를 수정해 S08 승패 분기를 독립 실행
- 드래그 이동 및 핀치 줌

## v4.61 전투·이벤트·스토리 범위

현재 피해 계산은 역분석에서 확인된 핵심 공방차를 사용해 `max(ATK - DEF, 0)`으로 결정적으로 처리합니다. 승패와 목표 전환은 `S_00.eex`에서 읽은 실제 인물 코드·좌표·턴 제한·목표 텍스트를 사용합니다.

아직 다음 요소는 적용하지 않습니다.

- 장비 공격/방어 보정
- 지형의 전투 능력 보정
- 병종 상성
- 명중/회피
- 치명타/연타
- 관통/특기
- Scene 2의 나머지 위치/인접/인원수 트리거 이벤트 자동 해석

전투 패널을 만드는 데 사용한 인물 오능, 직업 성장, 레벨, 초기 HP 원천값을 `battle0.json`에 함께 남겨 이후 실기/런타임 역분석 결과로 계산식을 교체할 수 있게 했습니다.

## 구현 방향

1. **데이터 카탈로그 작성**
   - 파일명/크기/해시/확장자/중복 여부 정리
2. **맵/전투 데이터 분석**
   - Mmap.e5, Pmap.e5, Pmapobj.e5, Hexzmap.e5, map/, RS/*.eex 분석
3. **그래픽/폰트/사운드 분석**
   - Unit_mov/Unit_atk/Unit_spc/Spalet, 이미지 리소스, 폰트, BGM/효과음 구조 분석
4. **Android 네이티브 렌더러**
   - Android Canvas 기반 프로토타입에서 시작해 필요한 경우 OpenGL ES로 확장
5. **게임 로직 재구현**
   - 이동 범위/지형, 전투 계산, AI, 이벤트, 메뉴, 세이브
6. **모바일 UI**
   - 터치 선택, 스크롤, 확대/축소, 뒤로가기, 전투 명령 UI
7. **원터치 APK**
   - 필요한 원본 데이터를 빌드 시 패키징하여 설치 후 바로 실행

- S14 중첩 `0x3F` 회합 조건을 공용 이벤트 엔진에 추가해 전장 이벤트 27/27 지원 기반 확보
- S14 Section 34/35/36 및 Scene 3 Section 1 종료 트리를 build-status에 전체 기록

- S14 Scene2 이벤트 27개의 트리거·액션 경로를 compact `eventRoutes`로 build-status에 기록
- 유비 탈출 좌표/승리 플래그 연결을 원본 이벤트에서 확정하기 위한 S14 route probe 추가
- `battle14.json`: M014 28×21/terrain14, R14 8인 roster, 원본 우군·적군 배치 패키징
- R14 Scene17 종료 직후 `battle14.json`을 로드해 실제 S14 전투로 연속 진입
- S14 Section11 유비(0) `(1,19)` 탈출 승리 루트를 원본 이벤트 그대로 실행
- S14 Section35 적 전멸 승리, Section34 유비 사망 패배, Section36 일반패배 실행
- S14 15턴 제한·유비 보호 및 Scene3 BGM/메뉴/팔레트 정리까지 연결


- S16 승리 후 원본 `R_17.eex` Scene 1~18 스토리 + Scene 19 출전 흐름을 실제 연속 재생
- R17 Scene19의 원본 출전 후보/고정 인물에서 8인 기본 roster 자동 추출
- `battle17.json`: M017 26×30/terrain17, 플레이어8·우군8·적군59 원본 배치 패키징
- S17 유비(0)·조조(36) 보호, 30턴, 외성 섬멸 목표와 원본 승리/패배 Section 패키징
- S17 중첩 `0x26` 영역 조건을 공용 conditionalTrigger로 실행
- S17 `0x21` 전장 물체 추가 연출을 이벤트 흐름에 보존
- S17 route probe로 외성 섬멸의 정확한 원본 종료 조건을 compact build-status에 기록
- S17 Section5가 외성 3개 적군 구역의 인원수 0을 확인해 변수7을 설정하는 원본 섬멸 조건 확정
- S17 Section16이 변수7을 소비해 원본 종료 연출을 수행하므로 Section16 완료를 정확한 승리 신호로 적용
- S17 전체 맵 적군 전멸을 승리 조건으로 쓰지 않도록 수정
- 원본 `R_18.eex` / `S_18.eex` 및 `M018`/Hexzmap entry18 구조를 다음 확장용으로 자동 진단
- R18 마지막 Scene의 `0x06/0x2D` 출전 명령과 후보 인물을 compact status로 별도 기록
- S18 초기배치·이벤트 지원률·승패 후보 전체 트리를 `v4.42-r18.json`에 소형 진단으로 기록
- `R_18.eex` Scene 1 스토리 + Scene 2 출전 흐름을 S17 승리 뒤 실제 재생
- `battle18.json` 생성: M018 22x18, 플레이어 9명, 적군 20명, 원본 S18 배치/AI 보존
- R18 종료 직후 battle18을 재로드해 S18 전투 실제 진입
- S18 일반 전장 이벤트는 종료 Section을 제외하고 공용 이벤트 엔진에서 전부 실행
- 여포(119) 격퇴 Section13, 초선(158) 격퇴 Section24, 일반 승리 Section30 연결
- 유비(0) 사망 Section18, 일반/턴초과 패배 Section31, Scene3 정리 연결
- 원본 `R_19.eex` / `S_19.eex` 및 `M019`/Hexzmap entry19 구조 자동 진단
- S19 초기배치·이벤트 지원률·승패 후보 전체 트리를 `v4.44-r19.json`에 소형 진단으로 기록
- `R_19.eex` Scene 1~28 스토리 + Scene 29 출전 흐름을 S18 승리 뒤 실제 재생
- R19 Scene29의 `0x06` 고정 출전 + `0x2D` 선택 후보를 원본 순서대로 9인 S19 roster에 반영
- `battle19.json`: M019 30×30/terrain19, 플레이어9명과 원본 우군·적군·증원 배치 패키징
- R19 종료 직후 battle19을 재로드해 S19 전투 실제 진입
- S19 일반 Scene2 이벤트는 종료 Section 30/31/46/47을 제외하고 공용 이벤트 엔진에서 실행
- S19 유비(0)·진등(172) 보호, 25턴, 적 전멸 승리 조건 적용
- S19 진등 사망 Section30·유비 사망 Section31·승리 Section46·일반패배 Section47·Scene3 정리 실행
- S19 종료 뒤 원본 `R_20.eex` / `S_20.eex` 존재·Scene/Section/명령 구조 자동 진단
- `M020` 존재 시 JPEG 크기와 Hexzmap entry20 지형을 probe 자산으로 패키징
- S20 초기 배치·목표·전장 이벤트 지원률·승패 후보 전체 트리를 `v4.46-r20.json`에 compact 기록
- S19 승리 후 `R_20.eex` Scene 1~9 + Scene10 출전 흐름 실제 연속 재생
- R20 강제 5인 출전을 S20의 5개 플레이어 슬롯에 원본 순서대로 배치
- `battle20.json`: M020 20×30/terrain20, 플레이어5명과 원본 적군 배치·AI 패키징
- R20 종료 직후 battle20을 재로드해 실제 S20 전투 진입
- S20 장비(2) 보호, 15턴, 유대 격파 목표 적용
- S20 일반 Scene2 이벤트 16/16 지원 및 Section3/18/19·Scene3 종료 흐름 실행
- S20 완료 다음 `R_21.eex` / `S_21.eex` / `M021` 구조 자동 진단
- R21 흐름 명령, S21 초기 배치·목표·전장 이벤트·승패 후보를 `v4.48-r21.json`에 compact 기록
- `battle20`의 `nextScenarioProbe/postS20Probe`를 compact status에 누락 없이 기록
- 원본 ZIP의 R/S 시나리오 파일 인벤토리를 수집해 20 이후 실제 첫 R/S 번호 자동 탐색
- R20 `0x11 target=41`은 내부 시나리오 인덱스로 별도 기록해 파일 번호와 분리
- S20 승리 후 `R_21.eex` Scene1~8 + Scene9 출전 흐름 실제 연속 재생
- R21 유비(0)+장비(2)+진도(17)+하후박(19)+간옹(26) 5인 roster를 S21 원본 슬롯에 배치
- `battle21.json`: M021 20×30/terrain21, 플레이어5·우군3·원본 적군/증원 패키징
- `0x5B` 전장 좌표 범위 하이라이트를 Android 전장에 재현해 S21 raw 이벤트 41/41 지원 기반 확보
- S21 초기 6턴/전멸/5인 생존 조건에서 Section2의 25턴 유비 탈출 또는 전멸 목표로 전환
- S21 Section4 유비 탈출 승리, Section48 전멸 승리, 43~47 개별 사망 패배, 49 일반패배, Scene3 정리 연결
- S21 다음 원본 `R_22.eex` / `S_22.eex` 존재·Scene/Section·명령 구조 자동 진단
- `M022` 존재 시 JPEG 크기와 Hexzmap entry22 지형을 probe 자산으로 패키징
- S22 초기 배치·목표·전장 이벤트 지원률·승패 후보를 compact build-status에 기록

- v4.51 compact status에서 누락된 `battle21.nextScenarioProbe/postS21Probe` 기록을 복구

- S21 승리 후 `R_22.eex` Scene1~22 스토리와 Scene23 출전 흐름 연속 재생
- `M022` 22×24 / terrain22 기반 `battle22.json` 실제 진입
- S22 관우 1인 플레이, 조조군 우군 11명, 원본 적군 36명 배치
- Data.e5 이름으로 안량 characterId 자동 탐색 및 원본 사망 Section 완료 후 승리 정산
- 관우(1)·조조(36) 사망, 20턴 초과 패배와 Section15/16/28/29·Scene3 정리 연결
- R22 `0x78` 무기/무기레벨/무기경험치 상태 전송 보존

## 다음 완료 기준

**S30 완료 뒤 실제 다음 R/S를 자동 탐색해 후속 시나리오 연속 플레이로 확장하는 것.**

- S22 종료 뒤 원본 `R_23.eex` / `S_23.eex` 존재·Scene/Section/명령 구조 자동 진단
- `M023` 존재 시 JPEG 크기와 Hexzmap entry23 지형을 probe 자산으로 패키징
- S23 초기 배치·목표·전장 이벤트 지원률·승패 후보를 compact build-status에 기록


## v4.57 R23 → S23 연속 플레이

- S22 승리 후 `R_23.eex` Scene 1~7 스토리와 Scene 8 출전 흐름을 원본 순서로 재생
- R23 출전 roster 6명: 유비(0) + 원본 출전 구조에서 확정된 5명
- `M023` 20×35 지도와 `terrain23.bin` 실제 로딩
- S23 아군 6명 / 우군 11명 / 적군 39명 원본 배치
- `0x6E` 확률 테스트를 중첩 조건으로 실행하여 S23 전장 이벤트 **33/33 지원**
- 승리: 조조(36) 격퇴 또는 적군 전멸
- 패배: 유비(0)·진진(25)·조운(4) 사망, 25턴 초과, 아군 전멸
- 원본 Section 33/35/36/37/40/41 종료 분기와 Scene 3 전투 후 정리 연결


## v4.58 R24 → S24 연속 플레이

- S23 승리 후 `R_24.eex` Scene 1~13 스토리 + Scene 14 출전 흐름 실제 재생
- `M024` / `terrain24.bin` 기반 `battle24.json` 생성 및 실제 S24 진입
- S24 플레이어 관우(1)·손건(29), 원본 우군·적군·증원 배치 보존
- `0x04` 문의 테스트를 즉시 예/아니오 선택 UI로 실행
- `0x4C mode1` 전장번호 기반 숨은 무장 등장 지원; S24 전장번호 1=손건(29) 문맥 검증
- 초기 10턴 `마차를 찾아라`에서 Section 3 후 80턴 `관우와 마차가 하북에 도착` 목표로 전환
- 관우(1)·마차(327) 사망 Section 83/84 패배, Section 102 승리, Section 103 일반패배, Scene 3 정리 연결
- 원본 `0x5B` 승리영역 `(0,0)~(22,2)`에 관우·마차가 모두 도달하면 승리 정산


## v4.59 S24 이후 자동 탐색

- 원본 ZIP의 전체 `R_*.eex` / `S_*.eex` 인벤토리에서 번호 24 이후 첫 R/S를 자동 탐색
- 다음 R/S의 Scene 수, Section 수, 명령 분포, 전환 명령과 텍스트 샘플 기록
- 다음 S의 초기 0x4A/4B/46/47 배치·목표·턴·작전개시 자동 진단
- 다음 S의 Scene2 이벤트 후보/지원률과 승패 후보 Section 자동 진단
- 다음 S 번호에 맞는 `mNNN.jpg`와 Hexzmap entry N을 자동 검증·probe 자산으로 패키징


## v4.60 S24 이후 compact probe

- v4.59의 대형 상태 파일에 있던 `postS24Probe`를 `v4.60-next.json`으로 분리
- 24번 이후 실제 첫 R/S 파일명과 번호, 다음 맵/지형, S 초기 배치·이벤트·승패 후보를 소형 상태로 기록


## v4.61 R25 → S25

- S24 승리 후 원본 `R_25.eex` Scene 1~16과 Scene 17 출전 흐름을 연속 재생
- R25 9인 출전 로스터를 원본 `0x06/0x2D` 순서로 구성
- `M025` 20×22 지도와 Hexzmap entry 25 지형을 `battle25.json`에 연결
- S25 플레이어 9명, 조건 중복을 제거한 우군, 원본 적군 43명 배치
- S25 일반 전장 이벤트 14개를 기존 네이티브 이벤트 엔진으로 실행
- 15턴 제한, 유비(0)·337 보호, 적 전멸 승리
- 패배 Section 10/11, 승리 Section 16, 일반 패배 Section 17, Scene 3 정리 실행


## v4.64 R26 → S26 연속 플레이

- S25 승리 뒤 원본 `R_26.eex` Scene 1~8 스토리와 Scene 9 출전 흐름을 연속 재생
- R26 변수 1051에 따라 원본 13인/12인 출전 roster와 대응 `0x4B` 슬롯을 런타임에서 선택
- `M026` 36×30 지도와 Hexzmap entry 26 `terrain26.bin` 실제 로딩
- S26 원본 우군·적군·증원 배치, AI, 레벨, 숨김 상태 보존
- S26 Section 9의 `0x1F` 카메라 이동 명령을 전장 중심 이동으로 실행
- 승리 루트 1: 유비(0)가 원본 탈출 좌표 `(1,28)` 도착 → Section 11 정산
- 승리 루트 2: 적군 전멸 → Section 55 정산
- 유비 사망 Section 37, 25턴 초과/일반 패배 Section 56, Scene 3 전투 후 정리 연결
- CI에서 `battle26.json`의 플레이어 분기·이벤트 지원률·승패 routeModel을 `v4.64-r26.json`에 기록


## v4.65 S26 이후 자동 탐색

- 원본 ZIP 인벤토리에서 번호 26 이후 첫 R/S를 자동 탐색
- 다음 R의 Scene/Section 구조와 마지막 출전 Scene의 0x06/0x2D/선택 명령 기록
- 다음 S의 초기 배치·목표·턴·Scene2 이벤트 지원률·승패 후보 자동 진단
- 다음 S 번호에 맞는 mNNN.jpg와 Hexzmap entry를 검증하고 probe 자산으로 패키징
- 결과를 `v4.65-next26.json`에 compact 기록


## v4.68 R27 → S27 연속 플레이

- S26 승리 후 원본 `R_27.eex` Scene 1~12 스토리와 Scene 13 출전 흐름 재생
- 변수 1051에 따른 원본 13인/12인 출전 roster와 두 `0x4B` 슬롯 그룹 보존
- `M027` 20×26 지도와 Hexzmap entry27 `terrain27.bin` 실제 로딩
- S27 원본 우군·적군의 숨김/증원/AI/레벨 배치 보존
- Scene2 일반 이벤트 40개 + 원본 승패 Section 26/37/51/52 연결
- 변수 `6` 또는 `8` 설정 시 `적 유인` 단계에서 `적군 격퇴` 단계로 전환
- 유인 완료 후 활성 적군 격퇴 시 Section 51 원본 승리/전리품 정산
- 유비(0) 사망 패배, 유인 완료 전 조운(4) 사망 패배, 15턴 초과/아군전멸 일반패배
- Scene 3 전투 후 정리 실행


## v4.69 S27 이후 자동 탐색

- 원본 ZIP 인벤토리에서 번호 27 이후 첫 R/S를 자동 탐색
- 다음 R의 Scene/Section 구조와 마지막 출전 Scene의 `0x06/0x2D/선택` 명령 기록
- 다음 S의 초기 배치·목표·Scene2 이벤트 지원률·승패 후보 자동 진단
- 다음 S 번호에 맞는 `mNNN.jpg`와 Hexzmap entry를 검증하고 probe 자산으로 패키징
- 결과를 `v4.69-next27.json`에 compact 기록


## v4.70 S28 route probe

- R28/S28 존재와 M028 40×24 지도/terrain28을 v4.69에서 확인
- S28 Scene2의 위치·영역·인원수·변수·승패·목표·하이라이트·턴 명령을 compact route로 기록
- 미지원 Section 9의 전체 트리와 `0x78` 파라미터를 별도 기록
- 탈출 좌표와 원본 속성 전송을 확정한 뒤 다음 버전에서 R28→S28 플레이를 연결


## v4.71 R28 → S28 연속 플레이

- S27 승리 뒤 `R_28.eex` Scene 1~14와 Scene 15 출전 흐름 재생
- R28 출전 후보는 관우(1)·조운(4)·손건(29)·장비(2); 범용 출전 선택 UI 전에는 원본 Section 순서 첫 후보를 두 번째 슬롯에 사용
- `M028` 40×24 / `terrain28.bin` 패키징
- S28 플레이어 2명, 적군 58명과 hidden/reinforcement 보존
- `0x78` AllCondition 26 = 보조장비 지원
- 유비 보조장비 코드 130(적로) 여부에 따라 Section 9의 특수 탈출/체포 분기 실행
- 최종 탈출 영역은 원본 `(4,4)~(5,5)`
- 변수 9 이후 목표/턴 제한을 35턴으로 전환
- Section 55 유비, 56 특수 NPC(338), 57 채모(156) 패배 / 58 승리 / 59 일반패배 / Scene3 정리 연결


## v4.72 S28 빌드 회귀 수정

- v4.71의 S28 자산 생성 블록이 공용 캐릭터/스프라이트 헬퍼보다 먼저 실행되던 순서 문제 수정
- S28 기능/승패/적로 분기는 변경하지 않고 빌드 파이프라인만 정상화
- 다음 단계는 S28 승리 뒤 실제 다음 R/S 자동 탐색 및 연속 플레이 확장


## v4.74 S28 승패 상태 회귀 수정

- S28 승패 상태 검사 블록을 `currentBattleLabel()`에서 `checkBattleState()`로 복원
- v4.73 Java 컴파일의 `missing return value` 회귀 제거
- S28 적로 분기·탈출 영역·Section 55~59 승패 로직은 변경 없음
- 다음 단계: S28 승리 후 실제 다음 R/S 자동 탐색 및 연속 플레이 확장


## v4.75 S28 label 회귀 최종 수정

- `currentBattleLabel()`에 남아 있던 중복 S28 승패 검사 블록 제거
- S28 승패 검사는 `checkBattleState()` 단일 위치에서만 실행
- Java `missing return value` 컴파일 회귀 최종 제거


## v4.76 S28 이후 자동 탐색

- S28 승리 이후 원본 ZIP에서 28보다 큰 첫 R/S 파일을 자동 탐색
- 다음 R/S의 Scene/Section/명령 구조와 다음 R 마지막 출전 Scene 명령을 compact status에 기록
- 다음 S 초기 배치·Scene2 이벤트 지원률·승패 후보를 자동 진단
- 다음 S 번호에 맞는 mNNN.jpg와 Hexzmap entry를 검증하고 probe 자산으로 패키징
- 결과를 `v4.76-next28.json`에 기록해 R29/S29 연속 플레이 구현 근거로 사용


## v4.77 R29 → S29 연속 플레이

- S28 원본 승리 후 `R_29.eex` Scene 1~21과 Scene 22 출전 흐름을 연속 재생
- R29의 원본 2지선다와 변수/무장 속성 변경을 기존 R 스토리 실행기로 처리
- Scene22 변수 `1051`에 따라 14인 / 13인 출전 roster를 원본 `0x06`에서 추출
- S29의 중복 `0x4B` slot 그룹을 roster 인원수에 맞춰 `playerVariants`로 연결
- `M029` 33×34 / `terrain29.bin`, 적 78명 및 hidden/reinforcement 상태 패키징
- S29 일반 Scene2 이벤트 63개를 기존 이벤트 엔진으로 실행
- 출전 아군 누구라도 사망하거나 25턴 초과 시 패배
- 조인(41) 사망 시 Section 70 원본 승리/도전/전리품 정산 실행
- Section 71/72 fallback 승패 및 Scene3 전투 후 정리 지원
- v4.75 회귀로 빠졌던 S28 승패 판정을 `checkBattleState()`에 복구


## v4.78 S29 이후 자동 탐색 및 누락 적군 진단

- S29 승리 이후 29보다 큰 첫 R/S 파일을 원본 ZIP에서 자동 탐색
- 다음 R/S Scene/Section/명령 구조, 다음 S 초기 배치·이벤트 지원률·승패 후보를 기록
- 다음 S 번호의 map/Hexzmap 지형을 검증해 probe 자산으로 패키징
- 다음 R 마지막 출전 Scene의 출전/선택/변수 명령을 compact status에 기록
- S29에서 일반 스프라이트 descriptor가 없어 제외된 250/253/254/255번 적군의 배치, job, mov/atk/spc descriptor와 시나리오 참조 위치를 기록
- 결과를 `v4.78-next29.json`에 저장해 누락 유닛 복원 및 R30/S30 구현 근거로 사용


## v4.79 S29 이후 probe 회귀 수정

- v4.78의 누락 적군 진단에서 잘못 호출한 `job_of()`를 공용 `job_profile_of()`로 교체
- S29 누락 적군 250/253/254/255의 job/sprite descriptor 진단을 정상화
- S29 이후 실제 다음 R/S 자동 탐색과 compact `next29` 상태 파일 생성을 복구



## v4.80 S29 특수 스프라이트 복원

- S29에서 빠졌던 여상(250)·하후존(253)·여상2(254)·적원(255)의 spriteId 265/268/269/270 원본 레코드 구조 확인
- 해당 mov/atk/spc 레코드는 표준 프레임 데이터보다 정확히 2바이트 긴 형식
- 공용 스프라이트 추출기가 `expected+2` 레코드의 2바이트 prefix를 제거한 뒤 표준 payload로 패키징
- 네 인물을 S29 원본 적군/증원 배치와 전장 이벤트에 다시 포함
- v4.79에서 확인한 다음 원본 경로는 `R_30.eex → S_30.eex`



## v4.81 R30/S30 원본 자산·탈출경로 probe

- `R_30.eex` Scene 1~26 스토리와 Scene 27 출전 흐름을 공용 R 스토리 컴파일러로 변환
- `S_30.eex`를 `battle30.json`으로 패키징
- M030 24×20 / Hexzmap entry 30 / Scene2 비종료 이벤트 20개 사용
- 유기(151) 사망 Section 18, 승리 Section 24, 일반 패배 Section 25, Scene3 정리 보존
- 유기 탈출의 정확한 position/area trigger를 native event에서 자동 찾아 `routeModel.escapeTriggerCandidates`로 기록
- 다음 단계에서 이 routeModel을 Android 상태기에 연결해 `S29 → R30 → S30` 연속 플레이를 완성



## v4.82 R30 → S30 연속 플레이

- S29 승리 후 `R_30.eex` Scene 1~26 스토리와 Scene 27 출전 흐름을 실제 재생
- R30 완료 직후 `battle30.json` / M030 / `terrain30.bin`을 로드해 S30 전투 진입
- S30 `routeModel.escapeTriggerCandidates`에서 유기(151)의 원본 탈출 좌표 `(1,18)`을 런타임에서 직접 읽음
- 유기가 `(1,18)`에 도달하면 원본 Section24 승리 정산, 적군 전멸도 원본 대체 승리조건으로 처리
- 유기 사망은 Section18, 20턴 초과/아군전멸/일반패배는 Section25 흐름으로 처리
- S30 Scene3 전투 후 정리까지 기존 outcome 실행기로 연결


## v4.83 S30 이후 원본 경로 자동 탐색

- S30 이후 존재하는 `R_*.eex` / `S_*.eex`를 번호순으로 탐색
- 첫 다음 R/S 파일의 EEX 구조와 텍스트/흐름 명령 자동 진단
- 다음 S 전투의 초기 배치·목표와 Scene2 이벤트 지원률·승패 후보 자동 진단
- 다음 S에 해당하는 지도 JPEG와 Hexzmap entry를 검증하고 지도/지형 자산을 미리 패키징
- 다음 R 마지막 Scene의 출전/선택/변수/분기 명령을 별도 기록


## v4.84 R31 → S31 연속 플레이

- S30 승리 후 `R_31.eex` Scene 1~18과 Scene 19 출전 흐름 재생
- R31 Scene19의 원본 `0x06` 고정 명단과 `0x2D` 후보를 source order로 합쳐 S31 10개 슬롯 구성
- `battle31.json`, M031 36×32, `terrain31.bin`으로 S31 실제 진입
- S31 전장 이벤트 40개는 기존 명령 집합으로 전부 해석 가능
- 유비(0) 퇴각 승리 위치/영역은 원본 event의 trigger + `battleEndMarker` 조합으로 자동 추출
- 유비 사망, 20턴 초과, 일반 패배와 Scene3 정리를 원본 outcome 흐름으로 처리


## v4.85 S31 이후 원본 경로 자동 탐색

- S31 승리 이후 31보다 큰 첫 R/S 파일을 원본 ZIP에서 자동 탐색
- 다음 R/S의 Scene/Section/명령 구조와 다음 R 마지막 출전 Scene 명령을 compact status에 기록
- 다음 S 초기 배치·Scene2 이벤트 지원률·승패 후보를 자동 진단
- 다음 S 번호에 맞는 mNNN.jpg와 Hexzmap 지형을 검증하고 probe 자산으로 미리 패키징
- 결과를 `v4.85-next31.json`에 기록해 R32/S32 연속 플레이 구현 근거로 사용


## v4.86 R32 → S32 네이티브 연결

- S31 승리 뒤 R_32.eex 3개 Scene을 네이티브 스토리로 연속 재생
- S_32.eex 강제 출전 0x4A에서 원본 3인 roster를 자동 복원
- m032.jpg(40×40)와 terrain32.bin을 네이티브 전장에 연결
- Scene2 원본 이벤트를 컴파일하고 승리 trigger를 원본 조건으로 평가
- 조운/간옹 사망 및 70턴 초과 패배 Section과 Scene3 후처리를 원본 흐름으로 연결
- 빌드 검증 결과를 `native/build-status/v4.86-s32.json`에 기록
